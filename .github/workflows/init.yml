name: CI
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: pip install --user mkdocs
    - run: find . -name mkdocs
    - run: mkdocs build && mkdocs gh-deploy --force
